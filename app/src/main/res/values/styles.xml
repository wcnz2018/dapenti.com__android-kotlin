<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#222222</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="colorAccent">#AAAAAA</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ColorTheme" parent="AppTheme">
        <item name="colorAccent">#69CBC4</item>
        <item name="android:windowAnimationStyle">@style/Animation.Activity.Style</item>
    </style>

    <style name="NightModeTheme" parent="ColorTheme">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textColorPrimary">#CCCCCC</item>
        <item name="android:textColorSecondary">#AAAAAA</item>

        <!-- same as getBackgroundColor() in model.Settings -->
        <item name="android:background">#424242</item>

        <item name="android:textColorAlertDialogListItem">#AAAAAA</item>
    </style>

</resources>
